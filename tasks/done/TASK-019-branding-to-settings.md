---
id: TASK-019
title: Бренд и юрлицо — в настройки
type: refactor
domains: [access]
status: done
created: 2026-09-05
---
## Сделано
- Ключ `general` расширен: `app_title`, `restaurant_name`, `company`, `logo_url`
  (валюта осталась, её очередь в TASK-020).
- `src/lib/branding.js` — загрузка при старте и сохранение; кеш и хелперы
  (`appTitle`, `venueName`, `copyrightLine`, `documentTitle`) в `config.js`.
- Заменено: сайдбар и мобильная шапка, экран входа с копирайтом, подзаголовок
  дашборда, шапка PDF, имя файла PDF (латиницей через `codeFromName`), текст
  WhatsApp, уведомление Telegram, подсказка про имя бота, заголовок вкладки.
- Экран «Настройки → Заведение».
- Тесты: нейтральные подписи без настроек, подстановка, пробелы, текущий год.
  184 теста.

## Что было неочевидно
Кеш в модуле не заставляет React перерисоваться, а экран входа рендерится до
того, как настройки догрузятся: он единственный не ждёт `loading`. Поэтому бренд
дополнительно лежит в сторе, и `LoginPage` на него подписан — иначе название
появлялось бы только после перезагрузки страницы.

## Не сделано
- `categorize.js` — правила с фамилиями и контрагентами, включая «Управляющая
  компания Мята», «RIM PARTNERS», «ЖК 4YOU». Это TASK-022.
- `tailwind.config.js` — зелёная палитра выведена из названия «Мята»; тема при
  white-label отдельная задача.

## Проверить руками
- «Настройки → Заведение»: поменять название приложения — оно должно смениться
  в сайдбаре и во вкладке браузера сразу, на экране входа после выхода.
- Отчёт смены: шапка PDF и текст WhatsApp с названием заведения.
