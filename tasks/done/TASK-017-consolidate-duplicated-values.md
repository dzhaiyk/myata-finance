---
id: TASK-017
title: Свести продублированные пороги и списки в один модуль
type: refactor
domains: [reporting, control, shift]
status: done
created: 2026-09-05
---
## Цель — что станет возможным
Каждое значение, которое сейчас скопировано в 3–5 файлов, лежит в одном месте под
именем, говорящим о назначении. После этого вынос в настройки (TASK-021) —
механическая работа, а не поиск копий по проекту.

## Не входит (out of scope)
- **Пороги не меняются**, кроме food cost и маржи: по ним владелец 05.09 принял решение
  (BR-RPT-018). Кассовые пороги, допуски счёта и наличных учредителей перенесены как есть.
- Экран настроек и таблица `settings` — TASK-021.
- Справочники отделов и категорий вместо `CHECK` — TASK-018.

## Контекст прочитан
- `docs/20-architecture/customization.md` — раздел «Пороги, размноженные копипастой»
- `docs/30-decisions/ADR-0010-nothing-venue-specific-in-code.md`
- `docs/10-business/shift/open-questions.md` #3 — пороги кассы 500 и 1000, открыт

## Затронутые правила
- Новых BR не вводится, существующие не меняются: поведение сохраняется полностью.

## Вопросы → ответы владельца (с датой)
- Взять TASK-017 → да (05.09.2026)
- Свести ли три порога food cost к одному → да, один набор границ (05.09.2026) → BR-RPT-018
- Какими будут границы → 35 % и 40 % (05.09.2026)
- P&L никогда не красит food cost зелёным → починить сейчас (05.09.2026)

## План
1. Расширить `src/lib/config.js` разделом порогов. Имена по назначению, а не по числу:
   расхождение кассы для отметки и для уведомления, допуск остатка счёта, допуск
   необъяснённых наличных учредителей, границы цвета food cost и маржи, порог аномалии
   food cost, порог доли ФОТ.
2. Свести списки категорий: `payroll_*` продублирован в `ControlPage.jsx:17`,
   `CashFlowPage.jsx:58`, `AnalyticsPage.jsx:185` и `:225-232`.
3. Заменить литералы на импорты в `DailyReportPage`, `DashboardPage`, `PnLPage`,
   `AnalyticsPage`, `ControlPage`, `AccountsPage`, `CashFlowPage`, `reconcile.js`.
4. Маржу привести к одним единицам: сейчас доли в дашборде и проценты в P&L при
   одинаковых границах.
5. Тест: значения в конфиге совпадают с теми, что были в коде до правки — защита от
   случайного изменения числа при переносе.

## Критерии приёмки — проверяемые, по одному на строку
- `grep -rn "500\|0\.35\|0\.32\|0\.40" src/pages` не находит порогов вне конфига.
- Ни одно число не изменилось: тест сверяет конфиг с зафиксированными значениями.
- `npm test` и `npm run build` зелёные.
- Экраны показывают те же цвета и предупреждения, что до правки.

## Тесты
- `config.test.js` — снимок значений порогов; падает, если число изменили.

## Сделано
- `src/lib/config.js`: `THRESHOLDS` (7 значений), `FOOD_COST_BANDS`, `MARGIN_BANDS`,
  `foodCostLevel`, `isFoodCostAnomaly`, `marginLevel`, `PAYROLL_CATEGORIES`.
- Литералы убраны из `DashboardPage`, `PnLPage`, `AnalyticsPage`, `ControlPage`,
  `AccountsPage` (5 копий допуска остатка), `CashFlowPage`, `DailyReportPage`
  (4 копии порога кассы), `reconcile.js` (5 значений по умолчанию).
- Список статей ФОТ был в четырёх местах, стал одним.
- Маржа приведена к долям: в P&L были проценты при тех же границах.
- Подписи на графиках и бейджах считаются из тех же значений — числа в тексте
  больше не расходятся с логикой.
- Тесты: 6 новых в `config.test.js`, снимок значений падает при случайной правке.
  Всего 160.

## Что изменилось для пользователя
- P&L: food cost теперь бывает зелёным (до 35 %); красный начинается с 40 %, а не с 32 %.
- Дашборд: красный food cost начинается с 40 %, а не с 45 %.
- Строки `fc_*` в P&L красятся по уровню, раньше были красными выше 32 % и серыми ниже.
- Аномалия в аналитике осталась на 40 % — не сдвинулась.
- Кассовые пороги, допуски счёта и наличных учредителей не изменились.

## Осталось
- Пороги живут в коде, а не в настройках — TASK-021.
- Не проверено в живом интерфейсе: авторизованные экраны из сессии недоступны.
  Владельцу стоит посмотреть P&L и аналитику после деплоя.

## Итог
**Сделано.** Пороги, границы и статьи ФОТ в `src/lib/config.js`; литералы убраны из
восьми файлов. BR-RPT-018: единая норма food cost 35/40, зелёный в P&L починен.
Маржа в долях. Подписи графиков считаются из значений. 160 тестов, снимок порогов
падает при случайной правке. Задеплоено, коммит `f41277e`.

**Не сделано.** Пороги остаются в коде — переезд в настройки это TASK-021.

**Проверить руками.** P&L и дашборд: зелёный food cost ниже 35 %, красный от 40 %
вместо 45 %. Смена цвета на значениях 40–45 % ожидаема, это не поломка.
