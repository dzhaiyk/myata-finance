---
id: TASK-026
title: Один источник кодов категорий — таблица categories
type: refactor
domains: [bank]
status: done
created: 2026-09-05
---
## Цель
Убрать копию списка статей из кода: приложение и контрактный тест опираются на
таблицу `categories` и её миграции (ADR-0010, ADR-0011).

## Что было
`CATEGORIES` в `categorize.js` — 60 кодов с подписями и группами. В приложении
не читался с TASK-027 (страницы берут статьи из справочника); держали его два
теста: контракт «каждый код есть в БД» и проверка сид-правил.

## Сделано
- `CATEGORIES` удалён вместе с устаревшим комментарием.
- `category-contract.test.js`: вместо списка кода проверяются служебные
  категории `NON_PNL_CATEGORIES` — единственный список кодов, оставшийся в коде.
- `categorize.test.js`: дубль проверки правил снят (её делает контракт по
  миграции 027). Всего 245 тестов.
- Документы: conventions, customization, bank/rules (источники без номеров
  строк), STATUS, BACKLOG, CHANGELOG.

## Не сделано
Номера строк `categorize.js:NNN` в источниках BR-BNK-006…010 устарели ещё с
TASK-022 (правила уехали в базу) — пересмотр этих правил отдельной задачей.

## Проверить руками
Ничего в интерфейсе не меняется. Импорт выписки и справочник статей работают
как прежде.
