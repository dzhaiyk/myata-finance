---
id: TASK-018
title: Отделы — справочник в базе вместо CHECK и названий в коде
type: refactor
domains: [shift, payroll, reporting]
status: done
created: 2026-09-05
---
## Цель — что станет возможным
Заведение заводит свои отделы само, не трогая схему и код. Смысл отдела перестаёт
зависеть от отображаемого названия.

## Не входит (out of scope)
- Справочник строк формы отчёта («Аппараты», «Табак», «Хозтовары») — остаётся в
  `config.js`, это последний остаток сопоставления по подписи.
- Типы оплат (Kaspi, Halyk, доставки) — TASK-019/021.

## Вопросы → ответы владельца (с датой)
- Модель хранения → одна таблица с флагами «выручка / персонал / закуп» (05.09.2026)
- Коды в 242 старых отчётах → проставить всем (05.09.2026)
- Экран управления → в этой же задаче (05.09.2026)

## Сделано
- Миграция 025: таблица `departments` (код, название, три признака, склад iiko,
  порядок, активность); значения в `positions`, `staff`, `suppliers` переведены
  с названий на коды; `CHECK` сняты, вместо них внешние ключи; в JSONB отчётов
  смен проставлен `code` с сохранением порядка элементов.
- `src/lib/config.js`: справочник кешируется, набора отделов в коде нет.
  `departmentsFor('revenue'|'staff'|'supply')`, `departmentCode`, `departmentLabel`,
  `departmentCodeByIikoStore`.
- `src/lib/departments.js`: загрузка при старте (`store.initialize`) и сохранение.
- `iiko.js`: склад → отдел через справочник; эвристика по словам («горячие»,
  «коктейль») убрана — она угадывала, а склад знает точно.
- Страницы: отчёт смены, персонал, поставщики — списки из справочника, хранится код.
- Экран «Настройки → Отделы»: добавить, переименовать, включить-выключить,
  указать склад iiko и порядок.
- Тесты: справочник подставляется фикстурой, как это сделает любое заведение.
  169 тестов.

## Не сделано
- Подписи разделов закупа в отчёте смены («Закуп Кухня») — отдельные строки в коде,
  с отделом связаны только кодом поставщика; переедут с TASK-019.
- Шаблон Telegram по-прежнему перечисляет три отдела поимённо (TASK-019).

## Проверить руками
- «Настройки → Отделы»: переименовать отдел и убедиться, что цифры в P&L не поехали.
- Отчёт смены: список отделов выручки; сохранить и открыть заново.
- Персонал и поставщики: отделы выбираются, в таблицах показаны названия.
