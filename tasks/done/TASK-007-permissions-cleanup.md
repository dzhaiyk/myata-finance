---
id: TASK-007
title: Права: мёртвые ключи и защита маршрутов
type: refactor
domains: [access]
status: done
created: 2026-09-05
---
## Цель
Каждый ключ прав что-то ограничивает; страница недоступна не только в меню,
но и по прямой ссылке.

## Вопросы → ответы владельца (05.09.2026)
- Мёртвые ключи → удалить `dashboard.kpi`, `daily_report.create`, `telegram.manage`;
  начать проверять `pnl.edit` и `cashflow.edit`.
- Маршруты → закрыть теми же правами, что и меню.

## Отклонение от ответа
`cashflow.edit` удалён, а не включён в проверку: у страницы Cash Flow нет ни одного
действия записи — проверять нечем. Вернуть ключ просто, когда правки появятся.

## Сделано
- `src/lib/routeAccess.js` (`permForPath`, `firstAllowedPath`, `canOpenPath`),
  `RouteGuard` и `IndexRedirect` в `App.jsx`, `NAV` экспортирован из `Layout.jsx`.
- `ALL_PERMISSIONS`: 33 → 29 ключей; группа Telegram снята со страницы ролей.
- P&L: кнопка «Редактировать» только с `pnl.edit`.
- Миграция 032 применена: строки `permissions` четырёх ключей удалены.
- BR-ACS-003/004 CONFIRMED, вопрос #2 закрыт, `reporting/permissions.md`, INDEX.
  2 теста, всего 251.

## Проверить руками
- Под ролью без `pnl.view` открыть /pnl по ссылке → переход на первую доступную.
- В базе `pnl.edit` только у Админа: Учредитель/Управляющий/Бухгалтер кнопку
  «Редактировать» в P&L не увидят — выдать право в «Роли и права», кому нужно.
- Страница ролей: групп «Telegram» и ключей KPI/create/cashflow.edit нет.
