---
id: TASK-015
title: Тихие поломки — сравнения по строкам заменить на устойчивые признаки
type: bug
domains: [reporting, access, shift]
status: review
created: 2026-09-05
---
## Цель — что станет возможным
Переименование отдела, строки формы или роли перестаёт молча искажать цифры и права.
Сейчас четыре места определяют смысл сравнением с отображаемым названием: ошибки нет,
результат просто уезжает не туда.

## Не входит (out of scope)
- Справочные таблицы отделов и категорий вместо `CHECK` — TASK-018.
- Вынос порогов и дублей в конфигурацию — TASK-017.
- Локализация, валюта, бренд — TASK-019…021.

## Контекст прочитан
- `docs/30-decisions/ADR-0010-nothing-venue-specific-in-code.md` — правила для нового кода
- `docs/20-architecture/customization.md` — раздел «Сравнения по строкам»
- `docs/10-business/shift/rules.md` — BR-SHF-019 (отдел из склада)

## Затронутые правила
- BR-SHF-019 — подтверждается: отдел определяется кодом, не названием.
- Новых BR не вводится: поведение расчётов не меняется, меняется способ сопоставления.

## Вопросы → ответы владельца (с датой)
- Принцип «ничего специфичного для заведения в коде» → да, ADR-0010 (05.09.2026)
- Взять TASK-015 первой → да (05.09.2026)

## План
1. Новый `src/lib/config.js` — единственный источник сопоставления: коды отделов
   (`kitchen`/`bar`/`hookah`/`other`) с их текущими названиями, функция
   `departmentCode(name)`, признак строк, уходящих в CapEx (сейчас «Аппараты»).
2. `src/lib/pnlCompute.js:140-143` и `:159` — звать `config.js` вместо сравнения строк.
   Неопознанное название больше не тонет: функция возвращает признак «не распознано»,
   чтобы вызывающий мог показать предупреждение.
3. `src/pages/CashFlowPage.jsx:100,189` и `src/pages/AnalyticsPage.jsx:137` — те же
   «Аппараты» из одного места вместо трёх копий литерала.
4. Миграция `024_role_is_superuser.sql`: `roles.is_superuser BOOLEAN NOT NULL DEFAULT false`,
   `UPDATE ... SET is_superuser = true WHERE name = 'Админ'`. **Применяется вручную в
   Supabase до пуша кода** — иначе администраторы потеряют доступ.
5. `src/lib/store.js:50,65` и `src/pages/RolesPage.jsx:37,48,81,179` — проверять
   `is_superuser`, а не имя роли.
6. `src/pages/UsersPage.jsx:20,46` — убрать `role_id: 4`; роль по умолчанию берётся
   первой из загруженного списка ролей, при пустом списке форма не отправляется.

## Критерии приёмки — проверяемые, по одному на строку
- [x] Переименование отдела не тонет молча: сумма уходит в «Прочее», а название
      попадает в `unknown_departments`, и вызывающий может это показать.
      *Исправлено против исходной формулировки: сохранить расчёт при переименовании
      нельзя, пока отдел хранится в отчёте как название без кода — это TASK-018.
      Здесь достигается только одно место правки и видимый сбой.*
- [x] Подпись строки CapEx задаётся в одном месте (`config.js`), форма отчёта берёт её оттуда.
- [x] Создание пользователя не зависит от порядка строк в таблице `roles`: роль выбирается явно.
- [x] Переименование роли «Админ» в базе не отнимает права: доступ даёт `is_superuser`.
      Автотестом не покрыто — `store.js` тянет `supabase` и `import.meta.env`, а тесты
      идут в `node:test` без Vite. Проверяется входом в приложение.
- [x] `npm run build` проходит.
- [x] `npm test` зелёный (153 теста).

## Ход работы
- 05.09 — шаги 1-3 и 6 сделаны: `src/lib/config.js`, `pnlCompute.js`, `CashFlowPage.jsx`,
  `AnalyticsPage.jsx`, `DailyReportPage.jsx`, `UsersPage.jsx`; тесты `config.test.js` и
  четыре новых в `pnlCompute.test.js`.
- 05.09 — миграцию 024 применить не удалось (заблокировано классификатором auto mode),
  файл готов, ждёт выполнения владельцем. Шаги 4-5 (`store.js`, `RolesPage.jsx`) после неё.
- 05.09 — уточнение по данным: `role_id: 4` — это «Менеджер», а не первая роль;
  первоначальный план «брать первую роль из списка» дал бы новому пользователю «Админ».
  Вместо этого роль выбирается явно.
- 05.09 — миграция 024 применена владельцем, проверено: `is_superuser = true` только у «Админ».
  Шаги 4-5 сделаны: `store.js` (загрузка роли, выдача полных прав, `hasPermission`),
  `RolesPage.jsx` (4 места). Суперролей может быть несколько — матрица прав это учитывает.
- 05.09 — удалён неиспользуемый `ROLES` в `UsersPage.jsx`: список из четырёх ролей,
  в котором не было «Бухгалтера».
- 05.09 — при вставке импорта в `AnalyticsPage.jsx` строка попала внутрь многострочного
  `import` и сломала сборку. Тесты этого не показали (покрывают только `src/lib`),
  поймал `npm run build`. Исправлено; в дальнейшем прогонять сборку после правок в `src/pages`.

## Что осталось владельцу проверить
- Войти под «Админ» и убедиться, что все разделы на месте: авторизованные экраны я
  проверить не могу — не ввожу чужие пароли.
- `unknown_departments` считается, но ни один экран его не показывает (отдельная правка).
- Цвета бейджей ролей в `UsersPage.jsx:78-85` по-прежнему привязаны к русским названиям.
  Это косметика с запасным вариантом, отнесено к TASK-019.

## Тесты
- `pnlCompute.test.js` — отдел с неизвестным названием попадает в «Прочее» и помечается
  нераспознанным; переименованный отдел с тем же кодом считается как прежде.
- `pnlCompute.test.js` — строка с признаком CapEx не попадает в Food Cost.
- `config.test.js` — `departmentCode` на всех текущих названиях и на неизвестном.
