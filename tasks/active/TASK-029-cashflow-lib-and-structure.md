---
id: TASK-029
title: Cash Flow — расчёт в src/lib с тестами, структура в базе (этап 3 ADR-0011)
type: refactor
domains: [reporting]
status: planned
created: 2026-09-05
---
## Цель — что станет возможным
Cash Flow считается чистой функцией с тестами, а не внутри страницы (закрывает
вопрос reporting #1). Строки CF и их подписи живут в базе рядом с P&L; группы
безналичных расходов берутся из структуры P&L, а не из семи списков в коде.

## Не входит (out of scope)
- Новые формулы CF из интерфейса — как и в P&L, только кодом.
- Справочник типов оплат: строка `type === 'Наличные'` остаётся до него
  (та же болезнь, что в `reconcile.js:23`), фиксируется как открытый вопрос.
- Дашборд и аналитика: свои копии списков категорий (`AnalyticsPage.jsx:229`)
  — TASK-005.

## Контекст прочитан
- `src/pages/CashFlowPage.jsx:14-248` — `CF_STRUCTURE` (35 строк), семь списков
  категорий, `computeMonthCF`: наличная выручка, наличные расходы по секциям,
  банк прямым методом по дате платежа, эквайринг, дивиденды, снятия, внутренние
  переводы нетто, CapEx с кальянными аппаратами из кассы, историческая замена.
- `docs/30-decisions/ADR-0011-dictionaries-module.md`, `TASK-028` (образец переноса).

## Что нашлось при чтении
- Месяц операции определяется через `new Date('YYYY-MM-DD').getMonth()` — это
  UTC-полночь, прочитанная в поясе браузера; восточнее UTC верно, западнее даст
  предыдущий месяц. В чистом модуле — по строке `YYYY-MM`, как в `acquiringByMonth`.
- Семь списков `*_CATS` дословно повторяют группы P&L; после TASK-028 они
  выводятся из `pnl_lines`: группа → её дочерние `category_code`.
- `cf_investments_in` обнуляется, если за месяц есть выписка, — существующее
  правило, переносится как есть.

## Вопросы → ответы владельца (с датой)
- Группы безналичных расходов: из `pnl_lines` или свой список → ?
- Объём: оба шага в одной задаче или только вынос расчёта → ?

## План
1. `src/lib/cashflowCompute.js`: `computeMonthCF` и `CF_STRUCTURE` переезжают
   из страницы без изменения логики; месяц — по строке даты. Страница только
   вызывает. Тест на фикстуре: наличная и безналичная части, эквайринг, дивиденды
   по банку и по журналу, историческая замена, знак каждой строки.
2. Тест эквивалентности шага 1: значения из нового модуля равны тому, что
   считала страница (снимок с той же фикстуры до переноса).
3. Группы безналичных расходов — из `pnl_lines` (если решение такое): функция
   `bankGroups()` собирает `group → [category_code]` из структуры P&L; тест
   сверяет её с прежними семью списками на seed.
4. Миграция 030: таблица `cf_lines` (ключ, подпись, уровень, раздел, `calc`,
   `parent_key`, порядок, видимость) — seed из `CF_STRUCTURE`, контрольная сумма
   по **полному** списку полей, снятому из данных, а не перечисленному вручную.
5. `cashflowStructure.js` по образцу `pnlStructure.js`; страница читает из него;
   вкладка «Строки Cash Flow» в «Справочниках» — подпись, порядок, видимость.
6. Документы: reporting/rules (BR-RPT-022), data-model, customization, CHANGELOG.

## Критерии приёмки — проверяемые, по одному на строку
- Cash Flow за любой месяц 2026 совпадает до тенге с прежним.
- `npm test` покрывает расчёт CF; страница логики не содержит.
- Переименование строки CF в «Справочниках» видно в отчёте, цифры прежние.
- Группы безналичных расходов не дублируются списками в коде.
- `npm test` и `npm run build` зелёные.

## Тесты
- `cashflowCompute.test.js` — фикстура месяца, каждая строка; `bankGroups` ↔ прежние списки.
- `cashflowStructure.test.js` — seed ↔ константа, скрытая строка, порядок.
