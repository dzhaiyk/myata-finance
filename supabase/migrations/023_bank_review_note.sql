-- Пометка «к проверке» на строках выписки: ставится при загрузке файла,
-- у которого не сошлись остатки (остаток начала + обороты ≠ остаток конца).
-- Снимается вручную на странице «Импорт выписки» после проверки.
ALTER TABLE bank_transactions ADD COLUMN IF NOT EXISTS review_note text;
