---
title: Сущности домена <домен>
summary: <сущности через запятую>: назначение, ключевые поля, статусы
read_when: <когда открывать>
domain: <домен>
status: draft
updated: <YYYY-MM-DD>
---

# Сущности

## <Сущность> (`<таблица>`)
**Назначение.** <Зачем существует.>

**Ключевые поля** (только бизнес-значимые):
- `<поле>` — <смысл>

**Состояния и переходы**
```mermaid
stateDiagram-v2
  [*] --> draft
  draft --> submitted: <кто>, <обратимо?>
```

**Инварианты**
- <утверждение, которое всегда верно> (BR-<DOM>-NNN)
