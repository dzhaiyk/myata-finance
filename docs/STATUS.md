# STATUS

Обновлено: 2026-09-05

## В работе
- Ничего не начато.

## Следующие
- TASK-024 — эквайринг без норматива (BR-CTL-018): сейчас код всё ещё сравнивает
  с 1,5 %, хотя решение принято.
- TASK-025 — привязать статьи `categories` к отделу.
- TASK-019 — бренд и юрлицо в настройки: `Layout`, копирайт на входе, шапка PDF
  и WhatsApp, `index.html`. После него в исходниках не останется названия заведения.
- TASK-020 — валюта, локаль, часовой пояс: `₸` строкой примерно в 60 местах,
  `ru-RU`, ввод чисел через запятую, явный TZ вместо времени браузера.
- TASK-021 — пороги из `src/lib/config.js` в таблицу `settings` и на экран.
- TASK-022 — правила категоризации и персональные данные из `categorize.js` в seed.
- `/discover payroll` — блок 1 из 12, ждёт ответов #6–11.
- TASK-002 (выписка Halyk ИП), TASK-003 (файл Kaspi) — ждут данных.

## Ждёт владельца
- **Проверить после деплоя:** переименованный отдел в P&L, разделах закупа, на
  дашборде и в Telegram; цифры при этом не должны сдвинуться. Роли под «Админ».
  Уведомления: выключить тип и убедиться, что сообщение не приходит.
- Вопросы домена shift: #8 (дата разделения типов оплат в iiko), #10 (чем
  заполнять Kaspi/Halyk за прошлые даты), #11 (доставка).
- Задание бухгалтеру: разделить типы оплат в iiko на Kaspi и Halyk.
- Ответы #6–11 в `docs/10-business/payroll/open-questions.md`.
- Разрешение удалить `docs/ФОТ-методика.md` (перенесено в payroll).
- Тест на реальном файле Kaspi: задваиваются ли операции «в обработке».

## Завершено недавно (последние 5)
- 05.09 — восемь порогов подтверждены: BR-SHF-001 (отсечка 06:00), BR-SHF-020
  (касса в ноль), BR-CTL-017 (учредители 200 000, выписка 3 дня), BR-CTL-018
  (эквайринг без норматива), BR-RPT-019 (маржа 30/15, ФОТ 30/35).
- 05.09 — TASK-023: переименование отдела видно в P&L, отчёте смены, на дашборде
  и в Telegram; код отдела автоматический и неизменяемый (правило 12).
- 05.09 — TASK-018: справочник `departments` (миграция 025), `CHECK` сняты,
  значения переведены на коды, добавлен экран управления.
- 05.09 — TASK-017: пороги и статьи ФОТ в `config.js`; BR-RPT-018 — единая норма
  food cost 35/40, зелёный в P&L починен.
- 05.09 — TASK-016: переключатели уведомлений работают, два несуществующих типа убраны.
- 05.09 — TASK-015 и TASK-014: суперправа по `is_superuser` (миграция 024),
  отдел выручки из склада iiko (BR-SHF-019), `/api/iiko` закрыт ключом.

## Известные проблемы
- См. `docs/50-ops/runbook.md`. Критичное: пароли в открытом виде, RLS открыт
  (закрывается на VPS, ADR-0008).
- Аналитика считает food cost и ФОТ своей копией логики, не через `pnlCompute`
  (TASK-005); фолбэк 0.5/0.3/0.2 по отделам подставляется молча.
- Захардкоженные значения заведения: `docs/20-architecture/customization.md`,
  осталось TASK-019…022.
