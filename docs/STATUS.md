# STATUS

Обновлено: 2026-09-05

## В работе
- Ничего не начато.

## Следующие
- TASK-019 — бренд и юрлицо в настройки: сайдбар, копирайт на входе, шапка PDF
  и текст WhatsApp, `index.html`. После неё в исходниках не останется названия
  заведения — самый заметный барьер перед продажей.
- TASK-020 — валюта, локаль, часовой пояс: `₸` строкой примерно в 60 местах,
  `ru-RU`, ввод чисел через запятую, явный TZ вместо времени браузера.
- TASK-021 — пороги из `src/lib/config.js` в таблицу `settings` и на экран.
- TASK-022 — правила категоризации и персональные данные из `categorize.js` в seed.
- `/discover payroll` — блок 1 из 12, ждёт ответов #6–11.
- TASK-002 (выписка Halyk ИП), TASK-003 (файл Kaspi) — ждут данных.

## Ждёт владельца
- **Проверить после деплоя:** «Импорт выписки» — статьи «ФОТ …» и «Закуп …»
  показывают новое название отдела. «Контроль» → «Эквайринг» — фактическая
  комиссия по банкам, отрицательная величина теперь норма. Касса: любое
  расхождение подсвечивается и уходит в Telegram. «Контроль» → наличные
  учредителей: −207 554 ₸ теперь вне допуска 200 000 и будет красным.
- Вопросы домена shift: #8 (дата разделения типов оплат в iiko), #10 (чем
  заполнять Kaspi/Halyk за прошлые даты), #11 (доставка), #9 (подтвердить, что
  «новое меню» — Кухня).
- Ответы #6–11 в `docs/10-business/payroll/open-questions.md` (блок 1 /discover).
- Задание бухгалтеру: разделить типы оплат в iiko на Kaspi и Halyk.
- Разрешение удалить `docs/ФОТ-методика.md` (перенесено в payroll).
- Тест на реальном файле Kaspi: задваиваются ли операции «в обработке».

## Завершено недавно (последние 5)
- 05.09 — TASK-025: статьи P&L следуют за переименованием отдела (BR-RPT-020,
  миграция 026). TASK-004 закрыта: терминалы задним числом не заполняем.
- 05.09 — TASK-024: эквайринг без норматива, показывается фактическая комиссия;
  вердикт не выносится, пока данные его не выдерживают (BR-CTL-018).
- 05.09 — восемь порогов подтверждены: отсечка 06:00, касса в ноль (BR-SHF-020),
  учредители 200 000 и выписка 3 дня (BR-CTL-017), маржа 30/15 и ФОТ 30/35
  (BR-RPT-019).
- 05.09 — TASK-023 и TASK-018: справочник отделов (миграция 025), переименование
  видно во всём приложении, код отдела автоматический и неизменяемый.
- 05.09 — TASK-014…017: iiko подключён, отдел из склада, суперправа по
  `is_superuser` (миграция 024), уведомления работают, пороги сведены в config.

## Известные проблемы
- См. `docs/50-ops/runbook.md`. Критичное: пароли в открытом виде, RLS открыт
  (закрывается на VPS, ADR-0008).
- Аналитика считает food cost и ФОТ своей копией логики, не через `pnlCompute`
  (TASK-005); фолбэк 0.5/0.3/0.2 по отделам подставляется молча.
- Захардкоженные значения заведения: `docs/20-architecture/customization.md`,
  осталось TASK-019…022.
- Сверка эквайринга без вердикта до накопления истории с сентября (BR-CTL-018).
