# STATUS

Обновлено: 2026-09-05

## В работе
- TASK-037 (изъятия из кассовой смены iiko) — код и правило BR-SHF-021 готовы; ждёт первого живого запуска
  на реальной смене (формат ответа iiko не подтверждён) и правил в справочнике «Изъятия iiko».

## Следующие
- **Переезд на свой сервер (ADR-0012)** — отложен до покупки VPS; план целиком в
  `docs/20-architecture/server-migration-plan.md`, задачи TASK-030…036. Текущее
  приложение живёт и правится — команда им пользуется.
- TASK-021 — пороги из `src/lib/config.js` в таблицу `settings` и на экран
  «Настройки»: механическая после TASK-017, значения уже подтверждены правилами.
- TASK-026 — `CATEGORIES` в `categorize.js` дублирует таблицу `categories`;
  контрактный тест переучить на таблицу и убрать константу.
- TASK-005 — аналитика считает food cost и ФОТ своей копией логики, не через
  `pnlCompute`; фолбэк 0.5/0.3/0.2 по отделам подставляется молча.
- Справочник типов оплат с кодами — если ответ на reporting #8 «да»: строка
  `'Наличные'` в Cash Flow и сверках, та же болезнь, что была с отделами.
- `/discover payroll` — блок 1 из 12, ждёт ответов #6–11.
- TASK-002 (выписка Halyk ИП), TASK-003 (файл Kaspi) — ждут данных.

## Ждёт владельца
- **Проверить после деплоя:** табель — импорт реального Excel (год первого листа
  2025), сверить «смены» по сотрудникам; выдать `timesheet.*` тем, кто заполняет.
  Cash Flow и P&L за любой месяц 2026 — цифры до тенге
  прежние; «Справочники → Строки P&L» и «Строки Cash Flow» — переименовать строку
  (видно в отчёте), скрыть (строки нет, итоги те же); вкладка «Статьи P&L»
  (подстатья, переименовать, отключить); «Настройки» без отделов; выдать
  `dictionaries.view` / `dictionaries.manage` ролям через «Роли и права».
- Вопросы reporting #8 (справочник типов оплат) и #9 (взносы учредителей в CF
  обнуляются при наличии выписки — правило или недосмотр).
- Вопросы shift #8, #10, #11, #9; payroll #6–11 — с прошлых сессий.
- Задание бухгалтеру: разделить типы оплат в iiko на Kaspi и Halyk.
- Разрешение удалить `docs/ФОТ-методика.md`; тест на реальном файле Kaspi.

## Завершено недавно (последние 5)
- 05.09 — TASK-038: табель смен в приложении с импортом из Excel и кнопкой
  «Из табеля» в зарплате; доля смены = множитель ставки (BR-PAY, миграция 031).
- 05.09 — TASK-029: Cash Flow вынесен в `src/lib` с тестами, строки и привязка
  статей в базе (миграция 030). ADR-0011 выполнен целиком (BR-RPT-022).
- 05.09 — TASK-028: структура P&L в базе (`pnl_lines`, миграция 029), редактор
  строк, формулы в коде (BR-RPT-021).
- 05.09 — TASK-027: модуль «Справочники» — отделы, статьи с подкатегориями
  (миграция 028), права `dictionaries.*`.
- 05.09 — ADR-0011 принят: справочники — модуль; структура отчётов — данные,
  формулы — код.
- 05.09 — TASK-015…022: разбор захардкоженного завершён — восемь задач, пять
  миграций; персональных данных и названия заведения в коде нет.

## Известные проблемы
- См. `docs/50-ops/runbook.md`. Критичное: пароли в открытом виде, RLS открыт
  (закрывается на VPS, ADR-0008).
- Сверка эквайринга без вердикта до накопления истории с сентября (BR-CTL-018).
- Привязка статей к строкам Cash Flow в интерфейсе только показывается.
- Сборка не ловит отсутствующий импорт — трижды за сессию поймано скриптом;
  проверять импорты после каждой правки страниц.
