# CHANGELOG

Что изменилось и почему, по задачам. Новое сверху. До введения TASK-нумерации (04.09.2026) записи сгруппированы по дням.

## 2026-09-06 · TASK-047 · наложение текста в карточке учредителя
- На вкладке «Учредители» суммы наезжали друг на друга: карточки стоят по три в ряд, а числа внутри раскладывались на четыре колонки по ширине экрана, а не карточки. На ячейку приходилось около ста пикселей при нужных ста десяти, число переносилось, знак валюты падал на отдельную строку. Крупный шрифт это обострил.
- Внутри карточки теперь две колонки, значения моноширинные и не переносятся, длинное имя учредителя не наезжает на кнопку передачи доли.
- Заодно доля от выручки в свёрнутых группах P&L получила тот же вид, что и в остальных строках.

## 2026-09-06 · TASK-046 · ещё крупнее шрифт, графики и проценты P&L
- Шкала кеглей поднята второй раз: основной текст приложения (`text-sm`, 350 мест) стал 16 пикселей вместо 15, подписи — 14, самый мелкий размер — 13. Первый заход был слишком осторожным.
- Графики дашборда и аналитики переведены на общее оформление (`chartTheme.js`): цвета берутся из палитры, поэтому подсказка перестала быть чёрной в светлой теме. Сетка только горизонтальная, подписи осей крупнее, цифры со столбиков убраны — их показывает подсказка.
- Доля от выручки в P&L из самого мелкого и блёклого текста строки стала выделенным показателем: крупнее, жирным, с отбивкой, а по food cost — в цвете нормы (BR-RPT-018). В таблице года колонка «% выр.» переехала из крайней правой сразу за название статьи, где видна без прокрутки на 1400 пикселей.

## 2026-09-06 · TASK-045 · крупнее шрифт, светлая тема, живой табель
- Шкала кеглей задана заново вместе с интерлиньяжем и трекингом: самый мелкий текст стал 12 пикселей вместо 9–11 (76 мест), основной — 15 вместо 14. Ни одного элемента мельче 12 пикселей не осталось.
- Светлая тема и переключатель в панели. Выбор помнится за пользователем: локально для мгновенного применения и в `app_users.theme`, чтобы уезжал на другое устройство (ADR-0013, миграция 033). Без выбора — как в системе.
- Табель переписан: ячейка-кнопка перебирает долю смены нажатием вместо трёхсот выпадающих списков, строки сгруппированы по отделам, шапка показывает день недели, выходные подсвечены, снизу итоги.
- В расчёте зарплаты названия отделов вместо кодов (`hall`, `bar`).
- Интерфейс шрифтом системы (на iPad это SF Pro), эмодзи в заголовках заменены иконками, цифры выровнены по разрядам, отклик на нажатие мгновенный, уважается системная настройка «меньше движения».

## 2026-09-06 · TASK-044 · смешанный чек больше не считается наличным целиком
- Выручка из iiko группируется по `PayTypes` вместо `PayTypes.Combo` (BR-SHF-022). Чек, оплаченный картой и наличными, приходил строкой «Банковские карты, Наличные», и правило узнавало в ней слово «налич» — весь чек шёл в наличную выручку.
- На данных 01.08–05.09.2026: наличные 9 418 996 → 9 022 702 ₸, карты 12 617 122 → 13 013 416 ₸, общая выручка не изменилась. Завышенные наличные завышали ожидаемый остаток кассы при пороге расхождения 0.
- Проверено: `NonCashPaymentType`, `CardType`, `PayTypes.Group`, `CardTypeName` эквайрера не различают — разделить Kaspi и Halyk задним числом из iiko нельзя (вопрос shift #10).

## 2026-09-06 · TASK-041 · одна кнопка «Заполнить из iiko» вместо двух
- Кнопка переехала в шапку отчёта и заполняет и выручку, и расходы за дату. Двух кнопок больше нет: они появились по-разному, а менеджер всё равно нажимал обе.
- Половины независимы: нет продаж или нет кассовых операций — вторая половина всё равно заполняется, о первой написано в плашке. Подтверждение одно и только о том, что действительно перезапишется.
- Запросы к iiko идут по очереди: у сервера одно соединение, параллельные вызовы заняли бы два слота лицензии.

## 2026-09-06 · TASK-037 · справочник «Изъятия iiko» заполнен, слово ищется с начала слова
- Живой запуск удался: из iiko пришли пять изъятий (261 800 ₸), все легли в «Прочие расходы» с пометкой, потому что правил не было.
- По словарю менеджеров заведены девять правил (табак, доставка табака, угли, аванс, кухня, бар и лёд, хозтовары, инкассация). «дост табак» стоит выше «табак», иначе доставка уехала бы в закуп табака.
- Слово теперь ищется с начала слова: «лед» больше не может поймать «следующая поставка» и увести деньги в бар, а «табак» по-прежнему ловит «табака». Браузеры без поддержки такого поиска работают как раньше.

## 2026-09-06 · TASK-037 · расходы из iiko: отчёт TRANSACTIONS вместо кассовых смен
- Кнопка «Расходы из iiko» падала с 404: методов `/resto/api/v2/cashshifts/*` в этом API нет, я взял их из документации, не проверив на сервере. Операции теперь берутся OLAP-отчётом `TRANSACTIONS` — тем же эндпоинтом, что и выручка.
- Имена полей отчёта спрашиваются у сервера (`olap/columns?reportType=TRANSACTIONS`) и выбираются из его списка; если список не отдался, берутся кандидаты по умолчанию, и плашка об этом пишет. Деньги из кассы отличаются от внесений по знаку суммы, а не только по названию типа операции.

## 2026-09-05 · TASK-040 · вкладка со старой версией чинится сама
- После деплоя открытая вкладка просила исчезнувший файл сборки и падала с «Failed to fetch dynamically imported module» (у владельца — на кнопке «Заполнить из iiko»). Теперь страница перезагружается сама, а заполненный черновик отчёта смены сохраняется перед перезагрузкой.
- Вторая перезагрузка подряд не запускается: при настоящей поломке пользователь увидит ошибку, а не вечный цикл.

## 2026-09-05 · TASK-039 · дашборд: выбор месяцев и группы расходов в чистой функции
- Выбор «закрытых» месяцев (BR-RPT-015), итоги года и помесячный ряд — `dashboardCompute.js` поверх `sumMonths`; подписи пирога расходов берутся из структуры P&L, а не из литералов на странице. Дублирующий расчёт food cost снят — показывается `fc_pct` из P&L. Цифры не меняются.

## 2026-09-05 · TASK-007 · права: маршруты закрыты, мёртвые ключи удалены
- Прямая ссылка на страницу без права просмотра ведёт на первую доступную; один список прав на меню и маршруты (BR-ACS-004).
- Удалены ключи, которые ничего не ограничивали: `dashboard.kpi`, `daily_report.create`, `telegram.manage`, `cashflow.edit` (у Cash Flow нет правок). Строки `permissions` с ними сняты миграцией 032.
- Ручные корректировки P&L теперь только с `pnl.edit`: в базе он есть лишь у суперроли — остальным ролям выдать при необходимости.

## 2026-09-05 · TASK-005 · аналитика считает той же формулой, что P&L
- Food cost (общий и по отделам), доля ФОТ и аномалии расходов в «Аналитике» считаются через `computeMonthValues` (BR-RPT-023). Своей копии логики у страницы больше нет; отдел ищется по коду, а не по названию «Кухня» — после переименования отдела график не ломается.
- Доли 50/30/20 для исторических месяцев без выручки по отделам убраны (BR-RPT-013 отменено): на графике пропуск. ФОТ теперь включает техперсонал из кассы, как в P&L — доля ФОТ за живые месяцы станет чуть выше прежней.
- Группы для аномалий расходов берутся из структуры P&L, не из списков категорий в коде.

## 2026-09-05 · TASK-026 · один источник кодов категорий
- Список `CATEGORIES` (60 кодов с подписями) удалён из `categorize.js`: приложение уже читало статьи из таблицы `categories`, копия в коде только расходилась с базой. Контрактный тест сверяет миграции таблицы с сидом правил, сидом P&L, группами Cash Flow и служебными категориями вне P&L.

## 2026-09-05 · TASK-021 · пороги и нормы в настройках
- «Настройки → Пороги и нормы»: допуски сверок (касса, остаток счёта, наличные учредителей, ФОТ) и границы показателей (food cost, маржа, доля ФОТ) правятся на экране и хранятся в `settings.thresholds`. В коде — только умолчания.
- Неверный набор (границы не по порядку, доля вне 0–100 %) не сохраняется и не применяется даже частично.

## 2026-09-05 · TASK-037 · расходы из кассовых смен iiko
- В отчёте смены кнопка «Расходы из iiko»: изъятия из кассовых смен за дату раскладываются по секциям по справочнику «Изъятия iiko» (Справочники → новая вкладка). Не распознанное — в «Прочие расходы» с пометкой, внесения пропускаются, ручные строки не трогаются, повтор не дублирует (BR-SHF-021).
- Прокси iiko: действия `cashshifts`, `cashshift_payments`. Формат ответа iiko не подтверждён — разбор терпимый, предпросмотр показывает пришедшие поля.

## 2026-09-05 · TASK-038 · табель смен вместо Excel
- Экран «Табель»: сетка сотрудники × дни за полмесяца, доля смены 1 / 0.7 / 0.5, штраф за период. Начисление = ставка × доля (BR-PAY).
- Импорт «штатного графика» из Excel: даты берутся из имени листа и порядка колонок, не из шапки — в файле чужие месяцы в заголовках, продублированный день и лист-копия; всё это импорт показывает как замечания. Телефоны и «наличка» не импортируются.
- В расчёте зарплаты кнопка «Из табеля»: подставляет смены и штрафы, поля остаются редактируемыми. `days_worked` стал дробным (миграция 031).
- Права `timesheet.view` / `timesheet.manage`, по умолчанию только у суперроли.

## 2026-09-05 · TASK-029 · Cash Flow: расчёт в src/lib, структура в базе
- `computeMonthCF` вынесен из страницы в `cashflowCompute.js` дословно и покрыт тестами; эквивалентность со старой реализацией из git проверена ключ за ключом. Закрыт вопрос reporting #1.
- Месяц операции — по строке даты: `new Date('YYYY-MM-DD').getMonth()` в поясе западнее UTC уводил первое число в прошлый месяц.
- Миграция 030: `cf_lines` (30 строк) и `cf_line_categories` (47 привязок, своя, не из P&L). Полный список полей снят из данных до генератора — урок TASK-028. Вкладка «Строки Cash Flow» в «Справочниках».
- ADR-0011 выполнен целиком. Заодно исправлена интерполяция в `fmtM` (печаталось «М {currencySymbol()}»).

## 2026-09-05 · TASK-028 · структура P&L в базе
- Миграция 029: таблица `pnl_lines` — 69 строк перенесены из `PNL_STRUCTURE` один в один, совпадение проверено контрольной суммой по всем полям. Формулы остались в коде (BR-RPT-021).
- Вкладка «Строки P&L» в «Справочниках»: переименовать, переставить, скрыть, привязать статью, добавить строку-статью. Скрытая строка остаётся в итогах.
- Тест эквивалентности: структура из seed и из константы дают одинаковые цифры ключ за ключом. Без него этап не закрывался бы.
- Первый генератор seed потерял поле `parent` у семи групп расходов — то же, что с условием «аренд» в TASK-022; поймал тест обратного преобразования, добавлена колонка `parent_key`.

## 2026-09-05 · TASK-027 · модуль «Справочники»
- Новый раздел в навигации: отделы переехали из «Настроек», появился экран статей P&L с подкатегориями (миграция 028, один уровень), отделом и шаблоном подписи. До этого статьями можно было управлять только SQL-ом.
- Поставщики, учредители, счета, персонал, роли, правила категоризации остались на своих страницах — в модуле карточки-ссылки.
- Права `dictionaries.view` / `dictionaries.manage`; по умолчанию только у суперроли (ADR-0011).

## 2026-09-05 · TASK-020 · валюта, локаль и часовой пояс — в настройках
- Символ валюты был вписан строкой в 153 местах, `ru-RU` — в двенадцати. Теперь деньги выводит одна функция `money()`, формат чисел и дат берётся из локали, знак валюты — из кода валюты (`settings.general`).
- Ввод сумм больше не навязывает запятую: разделитель берётся из локали.
- Операционная дата считается в часовом поясе заведения, а не по часам браузера. До этого менеджер, открывший отчёт в поездке, отнёс бы смену к другой дате. Для «Мяты» задан `Asia/Almaty`.
- Разбор захардкоженного завершён: все восемь задач (TASK-015…022) закрыты.

## 2026-09-05 · TASK-022 · правила категоризации переехали в базу
- 70 правил с фамилиями арендодателя, бухгалтера и учредителей, своим юрлицом, адресом и поставщиками Алматы убраны из `categorize.js` в `bank_rules` (миграция 027). Персональных данных в репозитории больше нет.
- Шаблоны сохранены как есть, оператором `matches`; совпадение проверено контрольной суммой md5 по (поле|шаблон|категория|направление|доп. условие).
- У `bank_rules` появился `sort_order`: правила читались без сортировки, то есть приоритет первого совпавшего зависел от порядка выдачи базы.
- В интерфейс правил добавлен оператор «по шаблону»: без него редактирование перенесённого правила сбросило бы его на «содержит».

## 2026-09-05 · TASK-019 · названия заведения в коде больше нет
- Название приложения, название заведения, юрлицо и логотип переехали в настройки (`general`) и подставляются в сайдбар, экран входа, копирайт, дашборд, шапку PDF, имя файла, WhatsApp и Telegram. Экран «Настройки → Заведение».
- Пока настройки не загружены, показывается нейтральное «Финансовый учёт»: чужого названия пользователь не увидит ни на секунду. Год в копирайте теперь текущий, а не зашитый 2025.
- В исходниках из брендового осталась только `categorize.js` (правила категоризации с контрагентами) — TASK-022.

## 2026-09-05 · TASK-025 · статьи P&L следуют за переименованием отдела
- Миграция 026: у статьи появились `department` и `name_template`. Восемь статей («Закуп …», «ФОТ …») собирают подпись из шаблона и текущего названия отдела — BR-RPT-020.
- TASK-004 закрыта решением владельца: терминалы за январь–февраль и Halyk до сентября задним числом не заполняются. Прошлые месяцы остаются «нет данных»; история для вердикта по эквайрингу накопится с сентября сама.

## 2026-09-05 · TASK-024 · эквайринг без норматива
- Ставка комиссии убрана из кода: сверка считает фактическую комиссию из данных (оборот по терминалу против зачислений) и показывает её по каждому банку. Прежние 1,5 % и допуск 1 % удалены.
- Вердикт «сошлось / не сошлось» не выносится намеренно. Проверка на данных 2026 показала: в нормальных месяцах комиссия Kaspi скачет от −0,8 % до +0,5 % и регулярно отрицательна — из-за задержки зачислений D+1..D+3, а январь–февраль вообще без терминалов. Порог при таких данных давал бы ложные тревоги.
- TASK-004 (заполнить терминалы) поднята до P1: без неё вердикт невозможен.

## 2026-09-05 · восемь порогов подтверждены владельцем
- BR-SHF-001: час отсечки 06:00 — заведение закрывается в 02:00, запас нужен админам на закрытие отчётов.
- BR-SHF-020: касса сходится в ноль. Вместо двух порогов (500 на подсветку, 1000 на Telegram) один, равный нулю: за 242 смены расхождение было 6 раз и не превышало 444 ₸, то есть прежние пороги не срабатывали ни разу.
- BR-CTL-017: допуск по наличным у учредителей ужесточён с 500 000 до 200 000 ₸; свежесть выписки 3 дня подтверждена.
- BR-CTL-018: ставка эквайринга не задаётся числом — фактическая комиссия считается из выписки и сравнивается с историей банка (в коде пока 1,5 %, TASK-024).
- BR-RPT-019: маржа 30/15, доля ФОТ — ориентир 30 %, тревога выше 35 %.
- Статьи `categories` решено привязать к отделу, чтобы название шло за переименованием — TASK-025.

## 2026-09-05 · TASK-023 · переименование отдела видно во всём приложении
- Строки P&L, привязанные к отделу (`rev_*`, `payroll_*`, `fc_*`, доли food cost), подписываются через `pnlLabel` по справочнику; разделы закупа в отчёте смены, графики дашборда и перечень отделов в Telegram — тоже.
- Код отдела больше не редактируется: создаётся из названия транслитерацией и фиксируется навсегда. Правило записано пунктом 12 в `claude.md` и в ADR-0010.
- Названия статей в `categories` за отделом не следуют: это отдельный справочник, связи «статья принадлежит отделу» в модели нет.

## 2026-09-05 · TASK-018 · отделы стали справочником
- Миграция 025: таблица `departments` вместо `CHECK` со списком русских названий в трёх таблицах. Заведение заводит свои отделы само, без правки схемы.
- Значения в `positions`, `staff`, `suppliers` переведены с названий на коды, вместо `CHECK` — внешние ключи. В 242 отчётах смен проставлен `code`.
- Отдел выручки из iiko определяется складом по полю `iiko_store` справочника; эвристика по словам («горячие», «коктейль») убрана — она угадывала, склад знает точно.
- Экран «Настройки → Отделы»: добавить, переименовать, включить-выключить, указать склад и порядок.

## 2026-09-05 · TASK-016 · настройки уведомлений стали настоящими
- Переключатели типов уведомлений сохраняются в `settings.telegram.notifications` и влияют на отправку; раньше это были `defaultChecked` без обработчика — вид настройки без настройки.
- Убраны два типа, которых не существовало: напоминание «отчёт не сдан до 02:00» и еженедельный алерт по food cost. В SPA без сервера выполнить их некому, вернутся с VPS.
- Bot Token и Chat ID больше не притворяются редактируемыми: показаны для чтения, токен замаскирован, кнопка «Тест» шлёт через общий отправитель.

## 2026-09-05 · TASK-017 · единая норма food cost и конец копипасты порогов
- Пороги, границы и список статей ФОТ сведены в `src/lib/config.js`: расхождение кассы было в пяти местах, допуск остатка счёта в пяти, наличные учредителей в трёх, статьи ФОТ в четырёх.
- BR-RPT-018: одна норма food cost — зелёный до 35 %, жёлтый 35–40 %, красный от 40 %; та же граница = аномалия. Было четыре несогласованных числа. Заодно починен P&L, где зелёный не показывался никогда.
- Маржа приведена к долям; подписи на графиках считаются из тех же значений, а не набраны текстом.

## 2026-09-05 · TASK-015 · тихие поломки по сравнению строк
- Суперправа по `roles.is_superuser` (миграция 024), а не по `name === 'Админ'`: переименование роли больше не снимает доступ. Роль нового пользователя выбирается явно вместо `role_id: 4`.
- Отделы и строка CapEx («Аппараты», была литералом в четырёх файлах) сопоставляются через `src/lib/config.js`; нераспознанное название не тонет молча.

## 2026-09-05 · TASK-014 · отдел выручки из склада iiko
- BR-SHF-019: отдел берётся из `Store.Name`, а не из `DishCategory`. Категория заполнена у 82,7 % выручки, склад — у 100 %; пятая часть выручки уходила в «Прочее» и разносилась вручную. Проверено на боевых данных: «Прочее» стало 0.

## 2026-09-05 · iiko подключён, прокси закрыт, курс на SaaS
- Доступ к iikoServer заработал; поля OLAP подтверждены. iiko не различает Kaspi и Halyk — решено разделить типы оплат на стороне iiko.
- `IIKO_PROXY_KEY` задан: до этого `/api/iiko` отдавал выручку любому запросу без ключа.
- ADR-0010 «ничего специфичного для заведения в коде» + правило 11 в `claude.md`; аудит `docs/20-architecture/customization.md`, разбор разложен на TASK-016…022.

## 2026-09-04 · инициализация документации
- Создана структура `docs/`, роутер `CLAUDE.md`, скиллы `/start` `/task` `/handoff` `/audit-docs` `/adr`, правила доменов.
- Старые документы (`CLAUDE.md`-энциклопедия, `AGENTS.md`, `Blueprint.md`, `docs/*.md`) перенесены по новой структуре.

## 2026-09-04 · контур наличных и учредители («Контроль»)
- Остаток наличных у учредителей на 1 января берётся из `settings.owner_cash_opening`; большой минус тоже красный.
- Дивиденды наличными (журнал инвестиций − выплаченное по банку) вычитаются из остатка у учредителей.
- Дни закрытия (`settings.closures`) не считаются пропущенными сменами.
- Данные: 3 млн «Фин помощь» 13.03 → возврат; ремонт августа 1 861 260 → CapEx вручную; 20 и 23 августа отделы приведены к оплатам по POS Halyk.

## 2026-09-04 · ежедневная загрузка выписок
- Конвейер импорта вынесен в `src/lib/bankImport.js`; карточка «Выписки банков» в отчёте смены; Telegram-сводка после загрузки.
- Файл с несошедшимися остатками сохраняется с пометкой `review_note` (миграция 023); пометка видна на «Импорте выписки» и «Контроле».
- При отправке отчёта с выпиской старше трёх дней приложение переспрашивает.

## 2026-09-04 · лимит 1000 строк Supabase
- Все объёмные выборки читаются постранично через `fetchAll`; годовой P&L перестал терять расходы с мая.
- Расчёт P&L вынесен в `src/lib/pnlCompute.js`, дашборд считает по тому же коду.

## 2026-09-04 · iiko и Halyk
- Прокси iiko (`netlify/functions/iiko.js`) поддерживает iikoServer API: вход по SHA1, токен в `key`, выход освобождает лицензию.
- Импорт PDF-выписки Halyk без внешних библиотек (`src/lib/pdfText.js`, `src/lib/halykStatement.js`) с проверкой оборотов и остатков.
- Cash Flow считается по дате платежа; снятие наличных и дивиденды — в финансовой деятельности.
- Категоризация: аренда только по получателю-арендодателю; налог на имущество и лайтбокс отдельными правилами; поступления не от эквайринга → «Доходы → Прочее».

## 2026-09-03 · аудит финансов 2022–2025
- Отчёт `docs/Аудит-финансов-2022-2025.md` и скрипты `tools/audit/`; исторический P&L переимпортирован; дивиденды 2022–2024 выровнены по Cash flow.
- Сверка эквайринга по банкам (Kaspi/Halyk); контур наличных: ЗП из кассы и от учредителей, техперсонал из смен.
- Методика ФОТ (`docs/ФОТ-методика.md`) для автоматизации модуля «Зарплаты».
